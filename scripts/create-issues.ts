import { PrismaClient } from '@prisma/client';

const prisma = new PrismaClient();

const issues = [
  // 5. 알림 시스템 (1개)
  {
    title: "브라우저 알림 권한 요청 기능 구현",
    priority: "MEDIUM" as const,
    domain: "feature-notifications",
    description: `## 목표
사용자가 작업 실행 완료/실패 시 브라우저 알림을 받을 수 있도록 알림 권한 요청 및 알림 전송 기능을 구현합니다.

## 배경/이유
LLM이 작업을 백그라운드에서 실행하는 동안 사용자가 다른 탭을 보거나 브라우저를 최소화할 수 있습니다. 작업 완료 시 알림을 통해 즉시 결과를 확인할 수 있도록 개선이 필요합니다.

## 구현 세부사항
1. \`web/src/hooks/useNotification.ts\` 훅 생성:
   - Notification API 권한 확인 및 요청
   - 알림 전송 함수 제공
   - 권한 거부 시 토스트 메시지 대체

2. \`web/src/components/NotificationPermissionBanner.tsx\` 컴포넌트 생성:
   - 최초 방문 시 알림 권한 요청 배너 표시
   - "허용" / "나중에" 버튼
   - localStorage에 사용자 선택 저장

3. \`web/src/components/TaskExecutor.tsx\` 수정:
   - 작업 완료 시 useNotification 훅 사용
   - 성공/실패에 따라 다른 알림 아이콘 표시
   - 알림 클릭 시 해당 Issue로 포커스 이동

4. 알림 내용:
   - 제목: \`[성공/실패] {Issue 제목}\`
   - 본문: \`{실행 결과 요약}\`
   - 아이콘: 성공(✅) / 실패(❌)

## 완료 조건
- [ ] 브라우저 알림 권한 요청 배너가 최초 방문 시 표시됨
- [ ] 사용자가 권한 허용 시 알림이 정상 동작함
- [ ] 권한 거부 시 토스트 메시지로 대체됨
- [ ] 작업 완료 시 알림이 전송됨 (성공/실패 구분)
- [ ] 알림 클릭 시 해당 Issue로 이동함
- [ ] TypeScript 타입 에러 없음
- [ ] 빌드 성공

## 제약사항
- Notification API가 지원되지 않는 브라우저는 graceful fallback (토스트)
- HTTPS 환경에서만 동작 (localhost 제외)
- 알림 권한은 사용자가 직접 허용해야 함 (자동 허용 불가)

## 참고사항
- Notification API 문서: https://developer.mozilla.org/en-US/docs/Web/API/Notifications_API
- 관련 파일: \`web/src/components/TaskExecutor.tsx\`
- 유사 기능: GitHub Actions 완료 알림, Vercel 배포 완료 알림`
  },

  // 6. 대시보드 (2개)
  {
    title: "작업 통계 대시보드 페이지 구현",
    priority: "HIGH" as const,
    domain: "ui-dashboard",
    description: `## 목표
Issue의 전체 통계를 시각화하여 보여주는 대시보드 페이지를 구현합니다.

## 배경/이유
현재는 Kanban 보드에서만 Issue를 확인할 수 있어 전체 프로젝트 상태를 한눈에 파악하기 어렵습니다. 대시보드를 통해 작업 현황, 우선순위 분포, 최근 활동을 쉽게 확인할 수 있도록 개선합니다.

## 구현 세부사항
1. \`web/src/pages/Dashboard.tsx\` 페이지 생성:
   - 레이아웃: 4개의 카드 섹션 (2x2 그리드)
   - 반응형 디자인 (모바일에서는 1열)

2. **상태별 통계 카드**:
   - TODO, ING, DONE, PENDING 개수 표시
   - 각 상태별 진행률 표시 (프로그레스 바)
   - 클릭 시 해당 상태 필터링된 Board로 이동

3. **우선순위별 분포 차트**:
   - Chart.js 또는 Recharts 사용
   - Doughnut 차트로 HIGH/MEDIUM/LOW 분포 표시
   - 색상: HIGH(빨강), MEDIUM(노랑), LOW(회색)

4. **최근 활동 타임라인**:
   - 최근 10개의 Execution 기록 표시
   - 시간, Issue 제목, 실행 결과 (성공/실패)
   - 실행 시간 표시 (예: "5분 전")

5. **릴리즈 히스토리**:
   - 최근 5개 릴리즈 목록
   - 릴리즈 날짜, 버전, 포함된 Issue 개수
   - 클릭 시 릴리즈 상세 페이지로 이동

6. \`web/src/services/api.ts\`에 API 추가:
   - \`GET /api/dashboard/stats\` - 전체 통계 조회

7. \`src/server/routes/dashboard.ts\` 라우터 생성:
   - Prisma로 집계 쿼리 작성
   - 캐싱 고려 (10초)

## 완료 조건
- [ ] 대시보드 페이지가 \`/dashboard\` 경로에 렌더링됨
- [ ] 상태별 통계가 정확하게 표시됨
- [ ] 우선순위 차트가 정확한 데이터로 렌더링됨
- [ ] 최근 활동 타임라인이 시간순으로 표시됨
- [ ] 릴리즈 히스토리가 최신순으로 표시됨
- [ ] 반응형 디자인 동작 (모바일에서 1열)
- [ ] TypeScript 타입 에러 없음
- [ ] 빌드 성공

## 제약사항
- Tailwind CSS만 사용 (별도 CSS 파일 금지)
- 차트 라이브러리는 Recharts 권장 (번들 사이즈 고려)
- API 응답 시간 500ms 이하 (집계 쿼리 최적화)

## 참고사항
- 디자인 참고: Linear Dashboard, Jira Reports
- Chart.js: https://www.chartjs.org/
- Recharts: https://recharts.org/`
  },
  {
    title: "대시보드 통계 API 엔드포인트 구현",
    priority: "HIGH" as const,
    domain: "api-dashboard",
    description: `## 목표
대시보드에서 사용할 전체 통계 데이터를 제공하는 API 엔드포인트를 구현합니다.

## 배경/이유
대시보드 페이지에서 여러 통계를 한 번에 조회할 수 있도록 집계 API가 필요합니다.

## 구현 세부사항
1. \`src/server/routes/dashboard.ts\` 파일 생성:
   - \`GET /api/dashboard/stats\` 엔드포인트 구현

2. Prisma 집계 쿼리 작성

3. \`src/server/index.ts\`에 라우터 등록

## 완료 조건
- [ ] GET /api/dashboard/stats 엔드포인트가 동작함
- [ ] 상태별 집계가 정확함
- [ ] 우선순위별 집계가 정확함
- [ ] 최근 실행 기록이 시간순으로 정렬됨
- [ ] 최근 릴리즈가 정확히 반환됨
- [ ] 응답 시간 500ms 이하
- [ ] TypeScript 타입 에러 없음
- [ ] 서버 빌드 성공

## 제약사항
- 집계 쿼리 최적화 (인덱스 활용)
- 대용량 데이터에서도 빠른 응답 보장

## 참고사항
- Prisma groupBy: https://www.prisma.io/docs/concepts/components/prisma-client/aggregation-grouping-summarizing
- 관련 파일: \`prisma/schema.prisma\``
  },

  // 7. 첨부파일 개선 (3개)
  {
    title: "이미지 첨부파일 미리보기 기능 구현",
    priority: "MEDIUM" as const,
    domain: "ui-attachment-preview",
    description: `## 목표
IssueDetailModal에서 이미지 첨부파일을 클릭하면 모달로 확대 미리보기를 제공합니다.

## 배경/이유
현재는 첨부파일을 다운로드해야만 이미지를 확인할 수 있어 불편합니다. 모달에서 바로 이미지를 미리볼 수 있도록 개선합니다.

## 구현 세부사항
1. \`web/src/components/ImagePreviewModal.tsx\` 컴포넌트 생성:
   - 이미지를 전체 화면 모달로 표시
   - 줌 인/아웃 버튼 (확대/축소)
   - 이전/다음 이미지 버튼 (여러 이미지 첨부 시)
   - ESC 키로 닫기
   - 배경 클릭으로 닫기

2. \`web/src/components/IssueDetailModal.tsx\` 수정:
   - 첨부파일 목록에서 이미지 타입 판별 (jpg, png, gif, webp)
   - 이미지 썸네일 표시 (100x100px)
   - 이미지 클릭 시 ImagePreviewModal 열기
   - 이미지가 아닌 파일은 다운로드 버튼만 표시

## 완료 조건
- [ ] 이미지 첨부파일 클릭 시 미리보기 모달이 열림
- [ ] 줌 인/아웃 버튼이 동작함
- [ ] 여러 이미지 첨부 시 이전/다음 버튼이 동작함
- [ ] ESC 키로 모달이 닫힘
- [ ] 배경 클릭으로 모달이 닫힘
- [ ] 이미지가 아닌 파일은 다운로드 버튼만 표시됨
- [ ] TypeScript 타입 에러 없음
- [ ] 반응형 디자인 (모바일에서도 정상 동작)

## 제약사항
- Tailwind CSS만 사용
- 이미지 로딩 실패 시 에러 메시지 표시
- 큰 이미지는 로딩 스피너 표시

## 참고사항
- 참고 디자인: Notion 이미지 미리보기, Discord 이미지 모달
- 관련 파일: \`web/src/components/IssueDetailModal.tsx\``
  },
  {
    title: "파일 다중 업로드 및 드래그 앤 드롭 구현",
    priority: "MEDIUM" as const,
    domain: "ui-file-upload",
    description: `## 목표
Issue에 여러 파일을 한 번에 업로드할 수 있도록 다중 선택 및 드래그 앤 드롭 기능을 구현합니다.

## 배경/이유
현재는 파일을 하나씩만 업로드할 수 있어 여러 파일 첨부 시 불편합니다. 드래그 앤 드롭으로 직관적인 업로드 경험을 제공합니다.

## 구현 세부사항
1. \`web/src/components/FileDropzone.tsx\` 컴포넌트 생성:
   - react-dropzone 라이브러리 사용
   - 드래그 앤 드롭 영역 표시
   - 여러 파일 동시 선택 가능
   - 파일 타입 제한 (이미지, PDF, 문서 등)
   - 파일 크기 제한 (기본 10MB)

2. \`web/src/components/IssueDetailModal.tsx\` 수정:
   - FileDropzone 컴포넌트 통합
   - 업로드 중인 파일 목록 표시 (프로그레스 바)
   - 업로드 완료 시 첨부파일 목록에 추가
   - 업로드 실패 시 에러 메시지 표시

## 완료 조건
- [ ] 여러 파일 동시 선택 가능
- [ ] 드래그 앤 드롭으로 파일 업로드 가능
- [ ] 파일 타입 제한이 동작함
- [ ] 파일 크기 제한이 동작함
- [ ] 업로드 중 프로그레스 바가 표시됨
- [ ] 업로드 실패 시 에러 메시지 표시
- [ ] TypeScript 타입 에러 없음
- [ ] 빌드 성공

## 제약사항
- react-dropzone 라이브러리 사용
- 파일 크기 제한은 환경변수로 설정 가능
- 업로드 실패 시 재시도 버튼 제공

## 참고사항
- react-dropzone: https://react-dropzone.js.org/
- 관련 파일: \`web/src/components/IssueDetailModal.tsx\`
- 디자인 참고: GitHub Issue 파일 업로드, Notion 파일 첨부`
  },
  {
    title: "파일 용량 제한 설정 기능 구현",
    priority: "LOW" as const,
    domain: "config-file-upload",
    description: `## 목표
환경변수로 파일 업로드 용량 제한을 설정할 수 있도록 구현합니다.

## 배경/이유
서버 스토리지 용량에 따라 파일 업로드 제한을 조정할 필요가 있습니다.

## 구현 세부사항
1. \`.env\` 파일에 환경변수 추가

2. \`src/server/middleware/upload.ts\` 파일 생성:
   - multer 미들웨어 설정
   - 파일 크기 제한 적용
   - 파일 타입 검증

3. \`src/server/routes/attachments.ts\` 수정:
   - upload 미들웨어 적용
   - 파일 크기 초과 시 413 에러 반환
   - 파일 타입 불일치 시 415 에러 반환

4. 클라이언트에서 제한 정보 조회:
   - \`GET /api/config/upload\` 엔드포인트 추가

5. \`web/src/components/FileDropzone.tsx\` 수정:
   - API에서 제한 정보 조회
   - 클라이언트 측 사전 검증
   - 제한 초과 시 에러 메시지 표시

## 완료 조건
- [ ] 환경변수로 파일 크기 제한 설정 가능
- [ ] 환경변수로 파일 타입 제한 설정 가능
- [ ] 서버에서 파일 크기 검증함
- [ ] 서버에서 파일 타입 검증함
- [ ] 클라이언트에서 사전 검증함
- [ ] 제한 초과 시 명확한 에러 메시지 표시
- [ ] TypeScript 타입 에러 없음
- [ ] 빌드 성공

## 제약사항
- multer 미들웨어 사용
- 기본값: 10MB, 이미지/PDF/문서

## 참고사항
- multer: https://www.npmjs.com/package/multer
- 관련 파일: \`src/server/routes/attachments.ts\``
  },

  // 8. 릴리즈 관리 개선 (3개)
  {
    title: "릴리즈 노트 작성 기능 구현",
    priority: "MEDIUM" as const,
    domain: "feature-release-notes",
    description: `## 목표
릴리즈 생성 시 마크다운 형식의 릴리즈 노트를 작성할 수 있는 기능을 구현합니다.

## 배경/이유
릴리즈마다 주요 변경사항, 버그 수정, 개선사항을 문서화할 필요가 있습니다. 릴리즈 노트를 통해 팀원들이 변경사항을 쉽게 파악할 수 있습니다.

## 구현 세부사항
1. \`prisma/schema.prisma\` 수정:
   - Release 모델에 \`notes\` 필드 추가 (String, nullable)
   - 마이그레이션 실행: \`npx prisma migrate dev --name add_release_notes\`

2. \`web/src/components/ReleaseModal.tsx\` 컴포넌트 생성:
   - 릴리즈 생성/수정 모달
   - 버전 입력 필드
   - 릴리즈 노트 마크다운 에디터 (react-markdown-editor-lite)
   - 미리보기 탭
   - 저장/취소 버튼

3. \`web/src/components/ReleaseDetailModal.tsx\` 컴포넌트 생성:
   - 릴리즈 상세 정보 표시
   - 릴리즈 노트 마크다운 렌더링 (react-markdown)
   - 포함된 Issue 목록
   - 수정/삭제 버튼

4. \`src/server/routes/releases.ts\` 수정:
   - POST /api/releases 수정 (notes 필드 추가)
   - PUT /api/releases/:id 추가 (수정 기능)
   - GET /api/releases/:id 추가 (상세 조회)

## 완료 조건
- [ ] Release 모델에 notes 필드 추가됨
- [ ] 릴리즈 생성 시 노트 작성 가능
- [ ] 마크다운 에디터가 정상 동작함
- [ ] 미리보기 기능이 동작함
- [ ] 릴리즈 상세 페이지에서 노트가 렌더링됨
- [ ] 릴리즈 수정 기능이 동작함
- [ ] TypeScript 타입 에러 없음
- [ ] 빌드 성공

## 제약사항
- react-markdown-editor-lite 사용
- react-markdown으로 렌더링
- 마크다운 XSS 방지 (sanitize)

## 참고사항
- react-markdown: https://www.npmjs.com/package/react-markdown
- react-markdown-editor-lite: https://www.npmjs.com/package/react-markdown-editor-lite
- 디자인 참고: GitHub Release Notes`
  },
  {
    title: "릴리즈 비교 기능 구현",
    priority: "LOW" as const,
    domain: "feature-release-compare",
    description: `## 목표
두 릴리즈 버전 간의 차이를 비교할 수 있는 기능을 구현합니다.

## 배경/이유
릴리즈 간 어떤 Issue가 추가/제거되었는지 확인할 필요가 있습니다.

## 구현 세부사항
1. \`web/src/pages/ReleaseCompare.tsx\` 페이지 생성:
   - URL: \`/releases/compare?from=v1.0.0&to=v1.1.0\`
   - 두 개의 릴리즈 선택 드롭다운
   - 비교 결과 표시 (추가/제거/변경된 Issue)

2. \`src/server/routes/releases.ts\`에 API 추가:
   - \`GET /api/releases/compare?fromId=xxx&toId=yyy\`

3. 비교 결과 UI:
   - 추가된 Issue: 녹색 배경
   - 제거된 Issue: 빨간색 배경
   - 공통 Issue: 회색 배경

## 완료 조건
- [ ] 두 릴리즈를 선택할 수 있음
- [ ] 비교 결과가 정확히 표시됨
- [ ] 추가/제거/공통 Issue가 구분됨
- [ ] TypeScript 타입 에러 없음
- [ ] 빌드 성공

## 제약사항
- Tailwind CSS만 사용

## 참고사항
- 디자인 참고: GitHub Compare Releases`
  },
  {
    title: "릴리즈 삭제 기능 구현",
    priority: "MEDIUM" as const,
    domain: "api-releases",
    description: `## 목표
릴리즈를 삭제할 수 있는 기능을 구현합니다.

## 배경/이유
잘못 생성된 릴리즈나 테스트 릴리즈를 삭제할 필요가 있습니다.

## 구현 세부사항
1. \`src/server/routes/releases.ts\`에 API 추가:
   - \`DELETE /api/releases/:id\`
   - 릴리즈와 연결된 Issue는 유지 (릴리즈만 삭제)
   - 삭제 전 확인 (최근 릴리즈인지 체크)

2. \`web/src/components/ReleaseDetailModal.tsx\` 수정:
   - 삭제 버튼 추가
   - 삭제 확인 다이얼로그 표시
   - 삭제 후 릴리즈 목록 새로고침

## 완료 조건
- [ ] DELETE /api/releases/:id 엔드포인트가 동작함
- [ ] 릴리즈 삭제 시 연결된 Issue는 유지됨
- [ ] 삭제 확인 다이얼로그가 표시됨
- [ ] 삭제 후 목록이 새로고침됨
- [ ] TypeScript 타입 에러 없음
- [ ] 빌드 성공

## 제약사항
- Cascade 삭제 방지 (Issue는 유지)

## 참고사항
- 관련 파일: \`src/server/routes/releases.ts\``
  },

  // 9. 성능 최적화 (4개)
  {
    title: "Issue 목록 무한 스크롤 구현",
    priority: "MEDIUM" as const,
    domain: "ui-board",
    description: `## 목표
Issue가 많을 때 초기 로딩 속도를 개선하기 위해 무한 스크롤을 구현합니다.

## 배경/이유
현재는 모든 Issue를 한 번에 로드하여 Issue 개수가 많아지면 초기 로딩이 느려집니다. 무한 스크롤로 필요한 만큼만 로드하여 성능을 개선합니다.

## 구현 세부사항
1. \`web/src/hooks/useInfiniteScroll.ts\` 훅 생성:
   - Intersection Observer API 사용
   - 스크롤 하단 감지
   - 다음 페이지 자동 로드

2. \`src/server/routes/issues.ts\` 수정:
   - 페이지네이션 파라미터 추가 (page, limit)

3. \`web/src/pages/Board.tsx\` 수정:
   - useInfiniteScroll 훅 사용
   - 각 컬럼별로 무한 스크롤 적용
   - 로딩 스피너 표시

4. 캐싱 전략:
   - React Query의 useInfiniteQuery 사용
   - 5분 캐싱
   - 백그라운드 refetch

## 완료 조건
- [ ] 초기 로딩 시 20개만 로드됨
- [ ] 스크롤 하단 도달 시 다음 페이지 로드됨
- [ ] 로딩 스피너가 표시됨
- [ ] 더 이상 데이터가 없을 때 로딩 중지됨
- [ ] 각 컬럼별로 독립적인 스크롤 동작
- [ ] TypeScript 타입 에러 없음
- [ ] 빌드 성공

## 제약사항
- React Query 사용
- Intersection Observer API 사용
- 초기 로드: 20개

## 참고사항
- React Query useInfiniteQuery: https://tanstack.com/query/latest/docs/react/guides/infinite-queries
- Intersection Observer: https://developer.mozilla.org/en-US/docs/Web/API/Intersection_Observer_API`
  },
  {
    title: "이미지 레이지 로딩 구현",
    priority: "LOW" as const,
    domain: "ui-image-loading",
    description: `## 목표
첨부된 이미지를 레이지 로딩하여 초기 페이지 로딩 속도를 개선합니다.

## 배경/이유
Issue에 첨부된 이미지가 많을 때 모든 이미지를 한 번에 로드하면 페이지가 느려집니다.

## 구현 세부사항
1. \`web/src/components/LazyImage.tsx\` 컴포넌트 생성:
   - loading="lazy" 속성 사용
   - Intersection Observer로 뷰포트 진입 감지
   - 로딩 중 placeholder 이미지 표시
   - 로딩 실패 시 에러 이미지 표시

2. \`web/src/components/IssueDetailModal.tsx\` 수정:
   - 이미지 렌더링 시 LazyImage 사용
   - 썸네일 blur placeholder

3. 이미지 최적화:
   - 이미지 크기 조정 (최대 1920px)
   - WebP 포맷 지원

## 완료 조건
- [ ] 이미지가 뷰포트에 진입할 때 로드됨
- [ ] 로딩 중 placeholder 표시됨
- [ ] 로딩 실패 시 에러 이미지 표시됨
- [ ] TypeScript 타입 에러 없음
- [ ] 빌드 성공

## 제약사항
- 네이티브 loading="lazy" 속성 우선 사용
- Intersection Observer fallback

## 참고사항
- 관련 파일: \`web/src/components/IssueDetailModal.tsx\``
  },
  {
    title: "API 응답 캐싱 구현",
    priority: "MEDIUM" as const,
    domain: "api-caching",
    description: `## 목표
자주 조회되는 API 응답을 캐싱하여 서버 부하를 줄이고 응답 속도를 개선합니다.

## 배경/이유
대시보드 통계, Issue 목록 등 자주 조회되는 데이터를 매번 DB에서 조회하면 불필요한 부하가 발생합니다.

## 구현 세부사항
1. \`node-cache\` 라이브러리 설치

2. \`src/server/cache.ts\` 파일 생성:
   - NodeCache 인스턴스 생성
   - TTL 설정 (기본 5분)
   - 캐시 키 관리

3. \`src/server/middleware/cache.ts\` 미들웨어 생성

4. 캐싱 대상 엔드포인트:
   - \`GET /api/dashboard/stats\` (5분)
   - \`GET /api/issues\` (1분)
   - \`GET /api/releases\` (5분)

5. 캐시 무효화:
   - Issue 생성/수정/삭제 시 캐시 삭제
   - Release 생성/수정/삭제 시 캐시 삭제

## 완료 조건
- [ ] node-cache 라이브러리 설치됨
- [ ] 캐시 미들웨어가 동작함
- [ ] 캐싱 대상 엔드포인트에 적용됨
- [ ] 캐시 무효화가 동작함
- [ ] 응답 속도가 개선됨 (로그 확인)
- [ ] TypeScript 타입 에러 없음
- [ ] 서버 빌드 성공

## 제약사항
- node-cache 사용
- TTL은 엔드포인트별로 다르게 설정

## 참고사항
- node-cache: https://www.npmjs.com/package/node-cache
- 관련 파일: \`src/server/routes/*.ts\``
  },
  {
    title: "웹소켓 실시간 업데이트 구현",
    priority: "HIGH" as const,
    domain: "feature-websocket",
    description: `## 목표
Socket.IO를 사용하여 Issue 변경사항을 실시간으로 모든 클라이언트에 반영합니다.

## 배경/이유
현재는 Issue가 변경되어도 다른 클라이언트에서 알 수 없습니다. 웹소켓으로 실시간 동기화를 구현합니다.

## 구현 세부사항
1. \`socket.io\` 설치

2. \`src/server/socket.ts\` 파일 생성:
   - Socket.IO 서버 설정
   - 이벤트 핸들러 정의

3. \`src/server/index.ts\` 수정:
   - Socket.IO 통합

4. 실시간 이벤트:
   - \`issue:created\` - Issue 생성
   - \`issue:updated\` - Issue 수정
   - \`issue:deleted\` - Issue 삭제
   - \`issue:status-changed\` - 상태 변경
   - \`execution:completed\` - 작업 실행 완료

5. \`web/src/hooks/useSocket.ts\` 훅 생성:
   - Socket.IO 클라이언트 연결
   - 이벤트 리스너 등록
   - React Query 캐시 무효화

6. \`web/src/pages/Board.tsx\` 수정:
   - useSocket 훅 사용
   - 이벤트 수신 시 UI 업데이트

## 완료 조건
- [ ] Socket.IO 서버가 동작함
- [ ] 클라이언트가 연결됨
- [ ] Issue 생성 시 모든 클라이언트에 반영됨
- [ ] Issue 수정 시 모든 클라이언트에 반영됨
- [ ] Issue 삭제 시 모든 클라이언트에 반영됨
- [ ] 작업 실행 완료 시 알림됨
- [ ] TypeScript 타입 에러 없음
- [ ] 빌드 성공

## 제약사항
- Socket.IO 사용
- CORS 설정 필요

## 참고사항
- Socket.IO: https://socket.io/
- 디자인 참고: Notion 실시간 협업, Figma 실시간 동기화`
  },

  // 10. 테스트 (3개)
  {
    title: "Issue CRUD API 단위 테스트 작성",
    priority: "MEDIUM" as const,
    domain: "test-unit-issues",
    description: `## 목표
Issue API 엔드포인트에 대한 단위 테스트를 작성합니다.

## 배경/이유
API의 정확성과 안정성을 보장하기 위해 테스트가 필요합니다.

## 구현 세부사항
1. \`jest\` 및 \`supertest\` 설치

2. \`jest.config.js\` 파일 생성

3. \`src/server/routes/issues.test.ts\` 파일 생성:
   - GET /api/issues 테스트
   - GET /api/issues/:id 테스트
   - POST /api/issues 테스트
   - PATCH /api/issues/:id 테스트
   - DELETE /api/issues/:id 테스트
   - PUT /api/issues/:id/status 테스트

4. \`package.json\`에 스크립트 추가

## 완료 조건
- [ ] Jest 설정 완료
- [ ] GET /api/issues 테스트 통과
- [ ] GET /api/issues/:id 테스트 통과
- [ ] POST /api/issues 테스트 통과
- [ ] PATCH /api/issues/:id 테스트 통과
- [ ] DELETE /api/issues/:id 테스트 통과
- [ ] PUT /api/issues/:id/status 테스트 통과
- [ ] 테스트 커버리지 80% 이상
- [ ] npm test 명령어 실행 성공

## 제약사항
- Jest 사용
- 테스트 DB는 메모리 DB 사용 (sqlite :memory:)

## 참고사항
- Jest: https://jestjs.io/
- Supertest: https://www.npmjs.com/package/supertest
- 관련 파일: \`src/server/routes/issues.ts\``
  },
  {
    title: "Kanban 보드 E2E 테스트 작성",
    priority: "MEDIUM" as const,
    domain: "test-e2e-board",
    description: `## 목표
Playwright를 사용하여 Kanban 보드의 주요 사용자 시나리오를 테스트합니다.

## 배경/이유
UI의 실제 동작을 검증하기 위해 E2E 테스트가 필요합니다.

## 구현 세부사항
1. \`@playwright/test\` 설치

2. \`playwright.config.ts\` 파일 생성

3. \`tests/e2e/board.test.ts\` 파일 생성:
   - Issue 생성 테스트
   - Issue 드래그 앤 드롭 테스트
   - Issue 수정 테스트
   - Issue 삭제 테스트
   - 필터링 테스트
   - 검색 테스트

4. \`package.json\`에 스크립트 추가

## 완료 조건
- [ ] Playwright 설정 완료
- [ ] Issue 생성 테스트 통과
- [ ] Issue 드래그 앤 드롭 테스트 통과
- [ ] Issue 수정 테스트 통과
- [ ] Issue 삭제 테스트 통과
- [ ] 필터링 테스트 통과
- [ ] 검색 테스트 통과
- [ ] npm run test:e2e 명령어 실행 성공

## 제약사항
- Playwright 사용
- 테스트 DB는 별도 DB 사용

## 참고사항
- Playwright: https://playwright.dev/
- 관련 파일: \`web/src/pages/Board.tsx\``
  },
  {
    title: "CI/CD 파이프라인 구축 (GitHub Actions)",
    priority: "LOW" as const,
    domain: "config-cicd",
    description: `## 목표
GitHub Actions를 사용하여 CI/CD 파이프라인을 구축합니다.

## 배경/이유
코드 변경 시 자동으로 테스트와 빌드를 실행하여 코드 품질을 보장합니다.

## 구현 세부사항
1. \`.github/workflows/ci.yml\` 파일 생성

2. \`.github/workflows/deploy.yml\` 파일 생성 (선택):
   - 배포 자동화 (Vercel, Netlify 등)

3. \`package.json\`에 스크립트 추가

## 완료 조건
- [ ] GitHub Actions workflow 파일 생성됨
- [ ] Push 시 자동으로 테스트 실행됨
- [ ] PR 시 자동으로 테스트 실행됨
- [ ] 테스트 실패 시 빌드 실패함
- [ ] 빌드 성공 시 배지 표시 (선택)

## 제약사항
- GitHub Actions 사용
- 모든 테스트 통과 필수

## 참고사항
- GitHub Actions: https://docs.github.com/en/actions
- 관련 파일: \`.github/workflows/ci.yml\``
  },
];

async function main() {
  console.log('Creating 18 issues...');
  let count = 0;
  
  for (const issue of issues) {
    const created = await prisma.issue.create({
      data: {
        title: issue.title,
        description: issue.description,
        priority: issue.priority,
        domain: issue.domain,
        status: 'TODO',
      },
    });
    count++;
    console.log(`✅ ${count}. Created: ${created.title}`);
  }
  
  console.log(`\n🎉 Successfully created ${count} issues!`);
}

main()
  .catch((e) => {
    console.error('Error:', e);
    process.exit(1);
  })
  .finally(async () => {
    await prisma.$disconnect();
  });
